{
  "name": "@zcode/update-service",
  "version": "1.0.0",
  "private": true,
  "description": "Mikiko 自建升级服务（Cloudflare Worker）：manifest 查询 + 强更配置 + R2 下载代理（spec specs/update/update-service.md）",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250913.0",
    "typescript": "^6.0.2",
    "wrangler": "^4.0.0"
  }
}
